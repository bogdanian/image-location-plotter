# image-location-plotter

To test the script you need to install pillow - https://pillow.readthedocs.io/en/latest/installation.html

python3 -m pip install --upgrade pip
python3 -m pip install --upgrade Pillow
